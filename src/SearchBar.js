import React, { Component } from 'react';
import './App.scss';

class SearchBar extends Component {
  render() {
    return (
      <input type="text" className="forem-control form-search"></input>
    )
  }
}

export default SearchBar;
